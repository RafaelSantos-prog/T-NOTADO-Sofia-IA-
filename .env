# Frontend Configuration
# VITE_BACKEND_URL é configurado automaticamente baseado no ambiente
# Em produção (Vercel): usa https://tnotado.vercel.app como fallback
# Em desenvolvimento: configure em .env.local